- name: Check if Elasticsearch service exists
  win_shell: Get-Service elastic* | Select-Object -ExpandProperty Name
  register: service_status
  ignore_errors: yes

- block:
    
  - name: Stop Elasticsearch service
    win_service:
      name: "{{ service_status.stdout_lines[0] }}"
      state: stopped

  - name: Remove Elasticsearch service
    win_shell: sc.exe delete "{{ service_status.stdout_lines[0] }}"
    ignore_errors: yes

  when: service_status.stdout_lines != []


- name: Create Elasticsearch directory
  win_file:
    path: "{{ elasticsearch_directory }}" 
    state: directory

- name: Check folder permissions for Elasticsearch
  win_acl:
    path: "{{ elasticsearch_directory }}"
    user: '{{ ansible_user }}'
    type: allow
    rights: ReadAndExecute

- name: Download Elasticsearch
  win_get_url:
    url: "{{ elasticsearch_download_url }}"
    dest: "{{ elasticsearch_zip_dest }}" 

- name: Unzip Elasticsearch
  win_unzip:
    src: "{{ elasticsearch_zip_dest }}"
    dest: "{{ elasticsearch_directory }}"
    creates: "{{ elasticsearch_unzipped_dir }}"

- name: Install Elasticsearch service
  win_shell: "{{ elasticsearch_service_install_command }}"
  args:
    executable: cmd

- name: Start Elasticsearch service
  win_service:
    name: "{{ elasticsearch_service_name }}"
    start_mode: auto
    state: started

- name: pause to allow service to start
  pause:
    seconds: 90

- name: Check user already exists
  win_shell: |
    {{ elasticsearch_bin_path }}\\elasticsearch-users.bat list
  args:
    executable: cmd
  register: elastic_users

- name: Add user
  win_shell: |
    {{ elasticsearch_bin_path }}\\elasticsearch-users.bat useradd {{ elastic_user }} -p {{ elastic_pwd }} -r superuser
  args:
    executable: cmd
  register: add_user
  until: add_user is not failed
  retries: 10
  delay: 10
  when: elastic_users.stdout_lines | select("search", elastic_user) | list | length == 0

- name: Wait for Elasticsearch to start
  win_uri:
    url: "{{ elasticsearch_url }}"
    method: GET
    status_code: 200
    url_username: "{{ elastic_user }}"
    url_password: "{{ elastic_pwd }}"
  retries: 2
  delay: 10
